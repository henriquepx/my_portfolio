import styled from 'styled-components';

const TrajectoryContainer = styled.div`
    height: 100vh;
    width: 100%;

    background-color: #161616;
    display: flex;
    align-items: center;
    justify-content: center;
`

const Trajectory = () => {
  return (
    <TrajectoryContainer>Trajectory</TrajectoryContainer>
  )
}

export default Trajectory